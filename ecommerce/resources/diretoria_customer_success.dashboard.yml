# Dashboard AI/BI "Diretoria de Customer Success" (gold). As consultas usam só o nome da tabela;
# catálogo e schema vêm de dataset_catalog/dataset_schema, para funcionar em dev e prod.

resources:
  dashboards:
    diretoria_customer_success:
      display_name: "Diretoria de Customer Success"
      file_path: ../src/dashboards/diretoria_customer_success.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: gold
